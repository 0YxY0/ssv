#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"

if ! command -v uv >/dev/null 2>&1; then
    echo "[SSV] 错误: 需要 uv 执行项目 CLI；请先安装 uv" >&2
    exit 127
fi

cd -- "$ROOT_DIR"
exec uv run --project "$ROOT_DIR" python "$ROOT_DIR/ssv.py" "$@"
